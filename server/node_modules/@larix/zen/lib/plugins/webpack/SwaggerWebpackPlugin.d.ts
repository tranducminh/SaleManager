import { Builder } from '../../Builder';
import Zen from '../../Zen';
export default class SwaggerWebpackPlugin {
    private _config;
    private _swaggerJsDoc;
    private _VirtualModules;
    private _builder;
    private _bail;
    private _pkgJson;
    constructor(builder: Builder, zen: Zen);
    apply(compiler: any): void;
}
