import { Builder } from '../../Builder';
import Zen from '../../Zen';
export declare const hasParallelLoalder: (builder: Builder) => boolean;
export declare const addParalleLoaders: (builder: Builder, zen: Zen, compilerRules: any) => any;
