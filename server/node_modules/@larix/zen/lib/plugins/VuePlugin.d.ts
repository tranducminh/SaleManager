import { Builder } from '../Builder';
import { ConfigPlugin } from '../ConfigPlugin';
import Zen from '../Zen';
export default class VuePlugin implements ConfigPlugin {
    configure(builder: Builder, zen: Zen): void;
}
